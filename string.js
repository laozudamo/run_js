/* 
  string
*/

const str = 'Hello World';
const arr = [1, 2, 3];
let v = str.slice(0, 5); // Hello
let k = arr.slice(0, 2); // [1, 2]
console.log(v, k);

let a = str.substring(0, 3); // Hello
console.log(a)

const j = arr.at(-1); // 3
console.log(j)